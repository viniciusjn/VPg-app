/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["media.rawg.io", "sm.ign.com", "www.gematsu.com", "image.api.playstation.com"], 
  },
};

module.exports = nextConfig;